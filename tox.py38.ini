[tox]
toxworkdir={env:TOX_WORK_DIR:.tox}
envlist = py{38}-django{30,31}

[testenv]
deps =
    django30: django==3.0
    django31: django==3.1
commands =
    poetry install
    {envpython} {toxinidir}/manage.py test --noinput
